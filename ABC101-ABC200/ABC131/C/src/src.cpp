#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <cmath>
#include <climits>
#include <string>

using namespace std;

#define rep(iter, limit) for (int iter = 0; iter < (int)(limit); iter++)
#define repp(iter, limit, init) for (int iter = (int)init; iter < (int)(limit); iter++)

/* 
CでもDでも割り切れない⇔!(Cで割り切れる, またはDで割り切れる)
*/

int main() {
	long int A, B, C, D;
	cin >> A >> B >> C >> D;

	if (C == 1 || D == 1) cout << 0 << endl;

	//vector<long int> c, d;


31 - 

	return 0;
}